
mod test {
    use crate::db::builder::BuildTable;
    use crate::db::table_cache::TableCache;
    use crate::util::options::Options;
    use crate::util::slice::Slice;

    #[test]
    fn test_build_table() {

        // BuildTable::build_table(&Slice::from("a"), Options::default(),
        //                         TableCache::new(),
        //                         Box::new());
        println!("get_name: {}", "a");

    }
}