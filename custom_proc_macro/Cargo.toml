[package]
name = "custom_proc_macro"
version = "0.1.0"
edition = "2021"

[lib]
# 过程宏
proc-macro = true
# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
syn = { version = "1.0", features = ["full"] }

[features]